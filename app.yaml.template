command:
  - streamlit
  - run
  - app.py
  - --server.port=$DATABRICKS_APP_PORT
  - --server.address=0.0.0.0
  - --server.headless=true

env:
  - name: LAKEBASE_DATABASE_URL
    value: postgresql://username:password@host/databricks_postgres?sslmode=require

resources: []
